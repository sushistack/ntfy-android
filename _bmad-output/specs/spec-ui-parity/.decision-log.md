# Decision Log — spec-ui-parity

## 2026-06-21 — Create

**Input:** `docs/ui-parity/` design folder (README, design-tokens, components, message-format,
screens-layout, CHANGELOG-redesign-2026-06, screenshots/). Mode: interactive (Jay), distilled
directly from rich input — no elicitation needed.

**Routing decision:** the five reference docs are mature, load-bearing catalogs authored upstream,
so they are **adopted companions** (referenced, not rewritten) per the companion rules — never
duplicated into the kernel. SPEC.md holds the 12-capability kernel that points into them.
`brownfield.md` is a **spec-authored** companion capturing the current `io.heckel.ntfy` app state
(View/XML, MainActivity=subscriptions + DetailActivity=messages) — load-bearing for downstream
architecture and not present in any input doc. README (index/org doc) and the playwright
re-capture instructions (process metadata) were dropped as wrapper-only. CHANGELOG adopted for its
delta/removal reference value.

**Self-validate Pass 1 (coherence, rules 1–6, 8):** PASS. All 12 capabilities carry intent+success;
IDs unique CAP-1..12; constraints each bend a decision (token-only, byte-match parser, no chart lib,
phone-first, security/fault-tolerance, sequenceId order); 8 explicit non-goals; success signal is a
concrete side-by-side parity test. Intents kept WHAT-only (HOW deferred to architecture; flagged in
the rendering-stack open question).

**Self-validate Pass 2 (preservation):** PASS. Token/component/protocol/layout catalogs preserved by
adoption; design-language gist mapped to CAP-2/3/4/12 + constraints; removed-features list mapped to
Non-goals + brownfield.md; screenshot-staleness preserved in the contract preamble + an assumption;
README parity-checklist preserved as capability success criteria. Wrapper-only drops: README ordering
index, playwright regen commands.

**Outputs:** SPEC.md (12 capabilities), brownfield.md, 6 companions (1 spec-authored + 5 adopted).
3 open questions logged (heuristic-kv timing, tablet rail scope, rendering stack).
